import React from "react";
import styled from "styled-components";

const TextInputWrapper = styled.input`
  background-color: white;
`;

export default function PrimaryButton({ label, value, setValue }) {
  return <TextInputWrapper />;
}
